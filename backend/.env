
# # const mongoose = require('mongoose');

# # const MONGO_URL = process.env.MONGO_URL || process.env.MONGO_URI || 'mongodb://127.0.0.1:27017/jobapp';

# # const connectDB = async () => {
# #   try {
  
# #     const opts = {
# #       useNewUrlParser: true,
# #       useUnifiedTopology: true,
      
# #     };

# #     await mongoose.connect(MONGO_URL, opts);
# #     console.log('MongoDB connected to', MONGO_URL);
# #   } catch (err) {
# #     console.error('MongoDB connection error:', err && err.message ? err.message : err);

# #     throw err;
# #   }
# # };

# # module.exports = connectDB;





# const mongoose = require('mongoose');

# // Prefer MONGO_URI only (Atlas). No local fallback.
# const MONGO_URI = process.env.MONGO_URI;

# const connectDB = async () => {
#   if (!MONGO_URI) {
#     console.error("❌ ERROR: MONGO_URI is missing in .env file.");
#     process.exit(1);
#   }

#   try {
#     const options = {
#       useNewUrlParser: true,
#       useUnifiedTopology: true,
#     };

#     await mongoose.connect(MONGO_URI, options);
#     console.log("✅ MongoDB Connected Successfully");
#     console.log("📌 Connected URI:", MONGO_URI.split("@")[1]); // Hide credentials
#   } catch (err) {
#     console.error("❌ MongoDB Connection Error:", err.message || err);
#     process.exit(1);
#   }
# };

# module.exports = connectDB;

MONGO_URI=mongodb+srv://ssk862309_db_user:Sahil1234@cluster0.s8k4ktr.mongodb.net/jobdb?retryWrites=true&w=majority
PORT=5001
